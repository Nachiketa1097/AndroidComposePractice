<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidComposePractice"
        tools:targetApi="31">
        <activity
            android:name=".study.activity.LifecycleActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AndroidComposePractice">

<!--            &lt;!&ndash; Main launcher intent &ndash;&gt;-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->

<!--            &lt;!&ndash; Combined SEND intent for text and image &ndash;&gt;-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.SEND" />-->
<!--                <category android:name="android.intent.category.DEFAULT" />-->
<!--                <data android:mimeType="text/plain" />-->
<!--                <data android:mimeType="image/*" />-->
<!--            </intent-filter>-->
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.AndroidComposePractice">

        </activity>

        <activity
            android:name=".study.activity.LifecycleActivity2"
            android:exported="false"
            android:theme="@style/Theme.AndroidComposePractice">

        </activity>

        <activity android:name=".study.activity.launchmode.LaunchModeMainActivity"
            android:exported="true"
            android:launchMode="standard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".study.activity.launchmode.StandardActivity"
            android:exported="true"
            android:launchMode="standard" />

        <activity android:name=".study.activity.launchmode.SingleTopActivity"
            android:exported="true"
            android:launchMode="singleTop" />

        <activity android:name=".study.activity.launchmode.SingleTaskActivity"
            android:exported="true"
            android:launchMode="singleTask" />

        <activity android:name=".study.activity.launchmode.SingleInstanceActivity"
            android:exported="true"
            android:launchMode="singleInstance" />
    </application>

</manifest>